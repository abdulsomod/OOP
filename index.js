const rectangle = {
  width: 10,
  height: 10,
  location: {x:0, y:0},
  draw(){
    console.log(`drawing....Spec => width: ${this.width}, height: ${this.height}`)
  },
};

rectangle.draw();
square.draw();

